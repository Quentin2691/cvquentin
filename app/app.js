// Récupérer le bouton
let bouton = document.querySelector('#add');
// Récupérer les email
let imput = document.querySelector('# email');
// click sur le bouton pour envoyer le message  
let button1 = function () {
    if (bouton = "submit") {
        console.log(bouton);
        let list = document.createElement('li');
        list.innerHTML = monInput;
    }

}
let newInput = function () {
    input.value = " ";
}


// mettre les écouteurs d'événement

let bouton = addEventListener("click")

$('.carousel').carousel({
    interval: 2000
})













